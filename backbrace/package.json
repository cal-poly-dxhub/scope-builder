{
  "name": "backbrace",
  "version": "0.0.1",
  "main": "src/app.js",
  "license": "MIT",
  "bin": {
    "backbrace-cli": "./src/app.js"
  },
  "author": {
    "name": "gusflus",
    "github": "github.com/gusflus"
  },
  "scripts": {
    "build": "tsc",
    "deploy": "yarn run build && cd cdk && cdk deploy",
    "local": "yarn run build && sam local start-api --port 3000",
    "setup": "cd scripts && sh setup.sh"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.161.1",
    "aws-sdk": "^2.1000.0",
    "constructs": "^10.3.0",
    "yaml": "^2.5.1"
  },
  "devDependencies": {
    "@types/jest": "^27.0.0",
    "@types/node": "^14.x.x",
    "jest": "^27.0.0",
    "ts-jest": "^27.0.0",
    "ts-node": "^10.x.x",
    "typescript": "^4.x.x"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  }
}
