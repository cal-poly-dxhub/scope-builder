{
  "name": "scope-builder",
  "version": "0.1.0",
  "bin": "bin/scope-builder.js",
  "scripts": {
    "build": "tsc",
    "deploy": "yarn run build && cdk deploy",
    "local": "yarn run build && sam local start-api --port 3000"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "22.7.9",
    "aws-cdk": "2.173.1",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "~5.6.3"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.713.0",
    "@aws-sdk/client-s3": "^3.713.0",
    "aws-cdk-lib": "2.173.1",
    "aws-sdk": "^2.1000.0",
    "constructs": "^10.3.0",
    "dotenv": "^16.4.7",
    "yaml": "^2.5.1"
  }
}
